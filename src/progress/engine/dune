(library
 (name progress_engine)
 (public_name progress.engine)
 (libraries
  (re_export mtime)
  (re_export optint)
  fmt
  logs
  logs.fmt
  terminal_ansi
  (select
   pvector.ml
   from
   (vector -> pvector.vector.ml)
   (!vector -> pvector.dynarray.ml))))

(library
 (name progress)
 (public_name progress)
 (foreign_stubs
  (language c)
  (names terminal_stubs))
 (libraries
  (re_export progress.engine)
  (re_export terminal)
  mtime.clock.os
  fmt
  unix))
